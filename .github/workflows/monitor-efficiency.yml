name: Monitor Documentation Efficiency

on:
  push:
    branches: [main]
  schedule:
    # Run weekly on Monday at 9am UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check documentation coverage
        id: coverage
        run: |
          # Count components in each doc set
          DESIGN_COUNT=$(find docs/components -name "*.md" 2>/dev/null | wc -l | tr -d ' ')
          STORYBOOK_COUNT=$(find docs-storybook/components -name "*.md" 2>/dev/null | wc -l | tr -d ' ')
          UNIFIED_COUNT=$(find docs-unified/components -name "*.md" 2>/dev/null | wc -l | tr -d ' ')

          # Export for later steps
          echo "design=$DESIGN_COUNT" >> $GITHUB_OUTPUT
          echo "storybook=$STORYBOOK_COUNT" >> $GITHUB_OUTPUT
          echo "unified=$UNIFIED_COUNT" >> $GITHUB_OUTPUT

          # GitHub Summary
          echo "## Documentation Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Doc Set | Components |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|------------|" >> $GITHUB_STEP_SUMMARY
          echo "| Design | $DESIGN_COUNT |" >> $GITHUB_STEP_SUMMARY
          echo "| Storybook | $STORYBOOK_COUNT |" >> $GITHUB_STEP_SUMMARY
          echo "| Unified | $UNIFIED_COUNT |" >> $GITHUB_STEP_SUMMARY

      - name: Detect missing documentation
        id: gaps
        run: |
          MISSING_COUNT=0
          MISSING_LIST=""

          for f in docs-storybook/components/*.md; do
            name=$(basename "$f" .md)
            if ! find docs/components -name "*.md" | xargs grep -l "name: $name" > /dev/null 2>&1; then
              MISSING_COUNT=$((MISSING_COUNT + 1))
              if [ $MISSING_COUNT -le 5 ]; then
                MISSING_LIST="$MISSING_LIST$name, "
              fi
            fi
          done

          echo "missing_count=$MISSING_COUNT" >> $GITHUB_OUTPUT
          echo "missing_list=${MISSING_LIST%, }" >> $GITHUB_OUTPUT

      - name: Check for stale unified docs
        id: staleness
        run: |
          UNIFIED_TIME=$(stat -c %Y docs-unified/index.md 2>/dev/null || echo 0)
          DESIGN_TIME=$(stat -c %Y docs/llms.txt 2>/dev/null || echo 0)
          STORYBOOK_TIME=$(stat -c %Y docs-storybook/llms.txt 2>/dev/null || echo 0)

          if [ "$UNIFIED_TIME" -lt "$DESIGN_TIME" ] || [ "$UNIFIED_TIME" -lt "$STORYBOOK_TIME" ]; then
            echo "stale=true" >> $GITHUB_OUTPUT
            echo "status=⚠️ Stale" >> $GITHUB_OUTPUT
          else
            echo "stale=false" >> $GITHUB_OUTPUT
            echo "status=✅ Up to date" >> $GITHUB_OUTPUT
          fi

      - name: Send Slack notification
        if: ${{ vars.SLACK_WEBHOOK_URL != '' || secrets.SLACK_WEBHOOK_URL != '' }}
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "📊 GDS Documentation Report"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Design Docs:*\n${{ steps.coverage.outputs.design }} components" },
                    { "type": "mrkdwn", "text": "*Storybook Docs:*\n${{ steps.coverage.outputs.storybook }} components" },
                    { "type": "mrkdwn", "text": "*Unified Docs:*\n${{ steps.coverage.outputs.unified }} components" }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Missing Design Docs:*\n${{ steps.gaps.outputs.missing_count }} components" },
                    { "type": "mrkdwn", "text": "*Unified Status:*\n${{ steps.staleness.outputs.status }}" }
                  ]
                },
                {
                  "type": "context",
                  "elements": [
                    { "type": "mrkdwn", "text": "🔗 <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View full report>" }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
