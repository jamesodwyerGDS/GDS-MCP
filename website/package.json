{
  "name": "gds-docs-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js --manifest-only && vite",
    "build": "node scripts/build.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
