{/* eslint-disable no-unused-expressions -- bc of GdsPortalRootId */}

import { Meta, Controls, Primary, Stories } from "@storybook/addon-docs/blocks";
import Modal from "../Modal";
import * as stories from "./modal.stories";
import { GdsPortalRootId } from "../../../hooks/usePortalElement";

<Meta title="Components/Modal" component={Modal} of={stories} />

# Modal

Importing the Modal component in your project:

`import { Modal } from "@ticketmaster/global-design-system";`

<Primary />

## Props

<Controls of={stories.Standard} />

## Rendering

Each Modal component is rendered as a portal to a wrapper element with the Id **{GdsPortalRootId}**, which is appended directly onto **document.body**.
This setup isolates the modal from the rest of the elements on the document to provide a consistent position for styling and managing z-index.

## Examples

<Stories title="" />
